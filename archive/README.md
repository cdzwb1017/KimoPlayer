# Archive

此目录保存未被当前构建入口引用的历史源码，移动前的内容没有删除。

- `legacy-source/main_fixed.js`：旧版主入口备份
- `legacy-source/main.js.pre-moji`：字符编码修复前的主入口备份
- `pre-modularization-20260723/`：模块拆分前的 `main.js`、`styles.css` 和 Vite 配置快照

当前正式入口仍为项目根目录下 `index.html` 引用的 `src/main.js`。
